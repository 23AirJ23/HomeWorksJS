export default class Book {
  constructor(author, title, genre, publisher, availability, origin) {
    this.author = author
    this.title = title
    this.genre = genre
    this.publisher = publisher
    this.availability = availability
    this.origin = origin
  }
}
